
const state = {
  userInfo: {}, //  user组件中使用
}
const mutations = {
  // 修改用户信息的数据
  // RECEIVE_USERINFO (state, userInfo) {
  //   state.userInfo = userInfo
  // }
}
const actions = {}
const getters = {}

export default {
  state,
  mutations,
  actions,
  getters
}